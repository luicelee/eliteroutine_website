# 설계 메모

코드만 봐서는 알 수 없는 **왜**를 남긴다. 규칙은 `CLAUDE.md`, 배포 절차는 `DEPLOY.md`.

---

## 1. 히어로 영상 장면표 (다시 뽑기 아까운 값)

영상 2편에 11개 종목이 들어 있다. 컷 지점은 `ffmpeg` 장면 검출로 확인했고,
간격이 규칙적이었다.

| 영상 | 길이 | 구성 | 장면당 |
|---|---|---|---|
| `heroplayer.mp4` | 10.00초 (240프레임 · 24fps · 1128x634) | 야구 · 축구 · 농구 · 태권도 · 펜싱 · 골프 | 1.667초 |
| `heroplayer2.mp4` | 10.00초 (240프레임 · 24fps · 1128x634) | 배구 · 조정 · 육상 · 스노보드 · 수영 | 2.0초 |

2026-08-20에 워터마크를 지우려고 재인코딩했지만 **프레임 수와 시각은 그대로다**
(크롭만 했고 자르거나 배속을 건드리지 않았다). 그래서 이 표는 유효하다.

이 값은 `index.html`의 각 `.nk-slide`에 `data-v` / `data-t` / `data-d`로 박혀 있다.
**영상을 갈아끼우면 반드시 다시 뽑아야 한다.** 방법:

```bash
pip install imageio-ffmpeg        # 컨테이너 기본 ffmpeg에는 H.264 디코더가 없다
FF=$(python3 -c "import imageio_ffmpeg;print(imageio_ffmpeg.get_ffmpeg_exe())")
$FF -i heroplayer.mp4 -vf "select='gt(scene,0.25)',metadata=print:file=-" -an -f null - \
  2>/dev/null | grep -oE "pts_time:[0-9.]+"
# 컷을 놓치면(크로스페이드는 점수가 낮다) 0.5초 간격 컨택트시트로 눈으로 확인:
$FF -i heroplayer.mp4 -vf "fps=2,scale=420:-1,tile=5x4" -frames:v 1 sheet.png
```

## 2. 카피 전환은 타이머가 아니라 영상 시각 기준

처음엔 5.6초 고정 타이머였고, 그래서 **화면 속 종목과 글자가 따로 놀았다.**
지금은 `requestAnimationFrame`으로 재생 중인 영상의 `currentTime`을 읽어 장면을 고른다.
버퍼링이나 탭 전환으로 영상이 밀려도 어긋나지 않는다.

`currentTime`은 재생 배속과 무관한 **매체 시각**이라, 0.8배로 낮춰도 위 장면표는 그대로 유효하다.

## 3. 재생 0.8배

장면당 1.667초에 두 줄 카피는 읽기 빠듯했다. 0.8배로 낮춰 체감 2.08초를 확보했다.
카피 등장 애니메이션도 0.62초로 당겨 글자가 빨리 자리잡게 했다.

`defaultPlaybackRate`까지 지정한 이유 — 두 번째 영상은 첫 영상이 재생을 시작한 뒤에
받도록 `load()`를 호출하는데(첫 화면에서 5.3MB를 한꺼번에 물지 않으려고),
`load()`가 `playbackRate`를 되돌린다. `defaultPlaybackRate`는 살아남는다.

## 4. 종목 × 기능 매핑

카피는 감성 문구가 아니라 **기능 하나씩을 업고 있다.** 종목을 추가하면 이 원칙을 지킬 것.

| 종목 | 카피 | 업은 기능 |
|---|---|---|
| 야구 | 어깨는 아껴 쓰는 것 / 루틴이 지킨다 | 투구 수 관리 |
| 축구 | 재능은 90분 / 루틴은 매일이다 | ACWR 훈련 부하 |
| 농구 | 더 높이보다 / 더 오래 뛰는 법 | 자세 분석 |
| 태권도 | 최고의 발차기는 / 스트레칭에서 나온다 | 유연성 루틴 |
| 펜싱 | 승부는 0.1초 / 준비는 사계절이다 | 성장 타임라인 |
| 골프 | 레슨비는 쓰는 게 아니라 / 남기는 것이다 | 훈련비 가계부 |
| 배구 | 스파이크는 한순간 / 어깨는 평생이다 | 통증 기록 |
| 조정 | 노를 젓는 건 팔이 아니라 / 매일이다 | 연속 루틴 |
| 육상 | 기록은 거짓말을 / 하지 않는다 | 전국 랭킹 |
| 스노보드 | 즐길 자격은 / 훈련이 만든다 | 루틴 아케이드 |
| 수영 | 물속의 시간도 / 기록으로 남는다 | 진학 실적표 |

`#sports`(종목 사례) 섹션도 같은 11종목을 같은 순서로 담는다.

## 5. 종목 색은 CSS 변수 하나로

`.nk[data-sport="xx"]`에 `--sport`(강조색) · `--sport-rgb`(틴트용) · `--vf`(영상 색보정)
세 개만 정의하면 카피 둘째 줄, 태그 점, 배경 틴트, 영상 필터가 전부 따라온다.
종목별로 규칙을 따로 쓰지 않는다.

영상 색보정은 **약하게** 건다. 실사 영상이라 `hue-rotate`를 세게 주면 피부톤이 무너진다.
무드는 주로 `.nk-tint` 방사형 그라디언트가 만든다.

## 6. 가격 표기 금지

구버전에는 "월 9,900원" 가격 섹션이 있었고 통째로 들어냈다. 출시 기념 전 기능 무료가
현재 방침이라 **금액을 다시 넣으면 안 된다.** 스모크 테스트가 이걸 감시한다
(`가격 표기 없음` 항목).

## 7. 검증 — 무엇을 보고 무엇을 안 보나

`npm test` (약 15초, 21개 항목). 노리는 것은 **단일 파일 · 단일 스코프의 연쇄 붕괴**다.
스크립트 위쪽에서 오타 하나 나면 그 아래 데모가 통째로 죽는데 화면은 멀쩡해 보인다.
실제로 히어로 JS 상단에 오타를 심어 세그먼트 · 재생속도 · 루틴 데모가 한꺼번에
잡히는 것을 확인했다.

**영상도 검증한다 — 단, 디코더가 있는 환경에서만.** 오래 "Playwright의 Chromium에는
H.264 디코더가 없다"고 적어두었는데, **Windows에서는 재생된다.** OS(Media Foundation)
디코더를 쓰기 때문이다. 2026-08-20 실측: readyState=4, 1280×720, 재생 진행 확인.
리눅스 컨테이너의 Chromium에는 여전히 없다.

그래서 `canPlayType('video/mp4; codecs="avc1.42E01E"')`로 능력을 먼저 물어본다.
빈 문자열이면 영상 6항목을 `SKIP`으로 넘기고 실패로 세지 않는다. 있으면 이걸 본다:

| 항목 | 무엇이 깨지면 걸리는가 |
|---|---|
| 실제 재생 · 길이 10.0초 | 영상을 교체하고 장면표(1번)를 다시 안 뽑은 경우 |
| 5.2s→태권도 · 8.6s→골프 | `data-t`/`data-d` 오타, 카피와 컷의 어긋남(2번) |
| 영상1 끝 → 영상2 (첫 장면 배구) | 크로스페이드 체인 |
| 영상2도 0.8배 | `load()`가 배속을 되돌리는 문제(3번)의 재발 |
| 세그먼트 클릭 → 그 장면 머리 | `nkSeek`, 그리고 서버의 Range 지원 |

마지막 줄 때문에 **스모크 테스트의 정적 서버가 Range 요청(206)을 지원한다.** 지원하지 않으면
브라우저의 `seekable.end(0)`이 0이 되어 탐색이 전부 실패한다 — `python -m http.server`로 한 번
헤맨 지점이고, Playwright의 `route.fulfill`도 같은 이유(Range 미지원)로 탐색 테스트에는 못 쓴다.
WebM으로 변환해 우회하던 검증 장비는 그래서 이제 필요 없다.

## 8. 성능

**실측(2026-08-20, routi.pages.dev 라이브):** 콜드 TTFB 52ms · FCP 420ms · load 528ms,
`heroplayer.mp4` 2.4MB 수신 81ms. 재방문은 load 407ms. 아래 걱정은 실측상 기우였다.

첫 화면에서 영상 5.3MB(2.4 + 2.9). 두 번째 영상은 첫 영상이 재생을 시작한 뒤 받는다.
`_headers`에서 `*.mp4`는 1년 immutable, `index.html`은 `max-age=0, must-revalidate` —
영상은 CDN이 오래 물고, HTML은 푸시하면 바로 갱신된다.

더 줄여야 하면: 720p 재인코딩, `poster` 이미지 추가, 2번째 영상 `preload` 낮추기.

## 9. 배포

`routi.pages.dev` (Cloudflare Pages, 프로젝트 이름 `routi`). `elite`는 전역 선점되어 있었다.
`main`에 push하면 자동 배포된다. 자세한 것은 `DEPLOY.md`.

## 10. 지면을 갈아엎었다 (2026-08-20)

히어로 영상은 세게 나오는데 **스크롤하면 갑자기 평범한 SaaS 랜딩으로 떨어진다**는 지적을
받았다. "멋있지 않고 AI가 만든 것 같다." 화면을 놓고 보니 원인이 분명했다.

| 무엇이 티를 냈나 | 어떻게 했나 |
|---|---|
| 다섯 섹션이 똑같은 문법(eyebrow → 2줄 헤딩 → 회색 서브 → 균등 카드) | 섹션마다 다른 레이아웃. 도판·챕터·인덱스·그리드 |
| 이모지 아이콘 20여 개 | 전부 제거. 기능 8종만 커스텀 SVG 라인 아이콘 |
| 배경과 구분 안 되는 어두운 카드 | 카드 폐기. 괘선(1px)으로 나눈다 |
| 균등 그리드 — 위계 없음 | 기능은 큰 것 1 + 작은 것 8. 챕터에는 거대 수치 |
| 파랑+민트 SaaS 조합 | 지면은 무채색, 강조 1색. 브랜드색은 앱 UI 안으로 |
| 떠 있는 글래스 칩 3개 | 폰 아래 괘선 캡션으로. 폰 화면을 가리던 버그도 같이 사라졌다 |
| 히어로 아래 전부 40/16/14px | display 최대 94px, 라벨 10px — 극단 대비 |

**히어로 영상 · 데모 4종 · 종목 11패널의 내용과 id는 한 글자도 건드리지 않았다.** 지면만
갈았다. 손으로 다시 치면 테스트가 의존하는 id가 어긋난다 — 기존 파일에서 뽑아 옮겨 붙였다.

### 사진은 어디서 왔나

외부 사진은 초상권 때문에 못 쓴다. **우리가 가진 히어로 영상이 유일하게 안전한 출처**라
장면당 한 프레임씩 뽑았다. 종목 스틸 11장(880px) + 풀블리드 도판 5장(1600px), 합쳐 544KB.

### 워터마크는 크롭으로 지웠다 (delogo가 아니라)

원본 영상에는 생성 워터마크(✦)가 **1280x720 기준 (1160, 595) 부근에 전 프레임 고정**으로
박혀 있었다. 두 가지를 시도했다.

- `delogo` — 사각형 얼룩이 남는다. 노을 그라디언트와 어두운 배경에서는 **워터마크보다 더
  눈에 띈다.** 못 쓴다
- **크롭** — 워터마크가 프레임 밖으로 나가도록 오른쪽 152px, 위아래 86px을 잘라
  1280x720 → 1128x634. 흔적이 0이고, 구도는 11장면 전부 눈으로 확인했다. 이걸 택했다

```bash
FF=/path/to/ffmpeg   # Windows 경로도 슬래시로 준다. 역슬래시는 셸에서 먹힌다
$FF -y -i heroplayer.orig.mp4 -vf "crop=1128:634:0:43" -c:v libx264 -crf 22 -preset slow -profile:v high -pix_fmt yuv420p -r 24 -movflags +faststart -an heroplayer.mp4
```

CRF 22에 업스케일 없음 — 원본(2.4+2.9=5.3MB)보다 가벼운 2.4+2.5=4.9MB가 된다.
1280x720으로 되돌리면 7.2MB까지 뛴다. 되돌릴 이유가 없다.

사진은 **재인코딩한 영상에서** 뽑는다(그래야 워터마크도 delogo 얼룩도 없다).

```bash
$FF -y -ss 0.90 -i heroplayer.mp4 -frames:v 1 -q:v 2 bb.jpg
$FF -y -i bb.jpg -vf "scale=880:-2" -quality 84 -compression_level 5 img/sport-bb.webp
```

프레임 시각은 1번 장면표의 각 컷 중간을 골랐다(예: 야구 0.90s, 태권도 5.90s, 육상 5.00s).
`img/*`는 `_headers`에서 1년 immutable로 캐시한다.

원본 영상은 `heroplayer.orig.mp4` 이름으로 저장소 밖(작업용 임시 폴더)에만 있다.
다시 손볼 일이 생기면 원본이 아니라 **현재 커밋된 영상에서** 시작해도 된다 — 크롭은
이미 끝났고 장면표는 그대로다.

## 11. 세그먼트 탐색이 라이브에서만 죽던 이유 (2026-08-20)

**Cloudflare Pages는 Range 요청에 206이 아니라 200 전체를 돌려준다.** mp4도 webp도
`Accept-Ranges` 헤더조차 없다. 확인:

```bash
curl -s -o /dev/null -w "%{http_code}" -H "Range: bytes=100-199" https://routi.pages.dev/heroplayer.mp4
# → 200 (206이어야 정상)
```

그러면 브라우저의 `seekable`이 **파일을 끝까지 받기 전까지 비어 있다.** 그 상태의
`currentTime` 대입은 예외도 없이 조용히 무시된다 — 세그먼트를 눌러도 아무 일이 없고
엉뚱한 장면이 뜬다. 7번에 적어둔 그 함정을 배포 환경에서 그대로 밟았다.

로컬 테스트 서버는 206을 주므로 **로컬에서는 절대 재현되지 않는다.** 배포 직후 CDN 캐시가
비어 영상 수신이 느릴 때 처음 드러났고, 느린 회선의 실제 사용자도 똑같이 겪는다.

`nkSeek`은 이제 대기-후-탐색이다. `seekable`이 목표 시각을 덮을 때까지 요청을 들고 있다가
가능해지면 건다(12초 후 포기). 기다리는 동안 **카피는 누른 장면에 고정**한다 — 안 그러면
엉뚱한 종목 문구가 몇 초간 보인다. `nkTick`이 `nkPending` 중에는 화면을 갱신하지 않는다.

재현하려면 로컬 서버를 Range 미지원으로 돌린다(작업용 `serve.mjs --no-range`). 그 조건에서
탐색까지 4.8초가 걸리지만 결국 정확히 도달하고, 그동안 카피는 어긋나지 않는다.

두 번째 영상이 아직 로드 전인 경우(8번의 지연 로딩)도 같은 경로로 해결된다.

스모크 테스트는 1번 세그먼트(영상1 0초 — 눌러도 티가 안 난다) 대신 **9번 세그먼트
(영상2 4.0초, 육상)** 를 눌러 확인한다.

## 12. 스토어 버튼은 대화상자를 연다

앱이 아직 스토어에 없다. 링크를 `href="#"`로 두면 눌러도 아무 일이 없어 고장으로 보인다.
"테스트 버전 배포 중" 안내와 문의 메일을 여는 대화상자로 바꿨다. 정식 등록되면 대화상자를
지우고 스토어 URL을 넣으면 된다(`[data-store]` 표시가 붙은 버튼 둘).

## 13. 만든 이유 카툰 Ver은 별도 페이지 (story.html)

같은 이야기를 만화 한 장으로 그린 것을 받았다. index.html 안에 끼워 넣으면 1024x1536짜리
그림이 지면의 리듬을 끊고, 글로 쓴 반성문과 정면으로 겹친다. **별도 페이지로 뺐다.**
링크는 내비게이션과 02 섹션 라벨 옆, 푸터 세 곳에 "만든 이유 카툰 Ver"로 걸었다.

- 원본 3.5MB PNG → **WebP 품질 92, 456KB.** 글자가 빽빽해서 86까지 내리면 획이 뭉갠다.
  무손실은 1.9MB로 값어치가 없다
- 만화는 흰 종이에 그려져 있다. 다크 지면에서 떠 보이지 않게 테두리를 두르고 앉혔다
- 좁은 화면에서는 글자를 읽을 수 없다. 그림을 누르면 원본 WebP가 새 탭에서 열린다
- 페이지 제목은 만화 안의 헤드라인과 겹치지 않게 다른 문장을 쓴다
  (처음엔 "다치지 않고, 오래오래 빛날 수 있도록"을 그대로 썼다가, 250px 아래 그림 안에
  같은 문장이 또 나와서 바꿨다)

**story.html은 index.html의 `<style>` 블록을 통째로 복사해 들고 있다.** 페이지가 둘뿐이라
공용 CSS 파일을 빼는 것보다 이 편이 단순하다(외부 의존성 없음 원칙도 지킨다). 대신 토큰이
서서히 갈라질 수 있어, 스모크 테스트가 두 페이지의 `--bg` · `--ink` · `--mark` · `--rule` ·
`--sans`를 직접 비교한다. index의 토큰을 고쳤다면 `<style>` 블록을 story.html에 다시 복사할 것.
