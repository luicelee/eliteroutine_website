# 엘리트 루틴 케어 홈페이지

- 단일 index.html (바닐라 JS, 외부 의존성 없음), Cloudflare Pages 배포 → routi.pages.dev
- 앱: 유소년 엘리트 선수 루틴·부상·진학 관리. **야구를 먼저 세우고 38개 종목을 다 안는다** —
  투구 수·구속·전력 분석은 야구 전용, 루틴·부상·진학은 전 종목 공통. 슬로건 "스스로, 루틴, 부상을 관리, 진학 리포트까지"
- 무드: 에디토리얼 다큐멘터리(잡지 지면). 다크(기본 #08090B)/라이트(웜페이퍼 #F5F2EC) 2테마
- 가격 표시 금지(무료 출시). 사진은 히어로 영상에서 뽑은 스틸(img/)만 — 외부 인물 사진 금지
- 수정 후 `npm test` (약 15초, 21개 항목). 콘솔 에러·영상·데모 4종·종목 탭·반응형·가격 표기를
  한 번에 본다.
  단일 파일이라 스크립트가 한 스코프로 이어져 있어, 오타 하나면 그 아래가 통째로 죽는다.
  화면은 멀쩡해 보이므로 눈으로는 놓친다 — 이걸 잡는 게 목적이다.
  영상 재생·카피 동기·크로스페이드·탐색도 본다. H.264 디코더가 없는 환경(리눅스 컨테이너의
  Chromium)에서는 그 6개 항목이 자동으로 SKIP된다 — Windows는 OS 디코더를 써서 실제로 재생된다
- 참고: k-elite.github.io / eliteroutine.github.io

**작업 전 [DECISIONS.md](DECISIONS.md)를 읽을 것.** 히어로 영상 장면표(다시 뽑기 비싸다),
카피가 업고 있는 기능 매핑, 검증에서 일부러 뺀 것과 그 이유가 들어 있다.

## 디자인 원칙 — 절대 훼손 금지

- 무드: **에디토리얼 다큐멘터리.** 잡지 지면처럼 괘선으로 나누고, 사진이 유일한 색이다.
  히어로 영상은 캠페인, 그 아래는 지면 — 이 대비가 이 사이트의 전부다
- **금지: 카드, 둥근 모서리, 그라디언트, 이모지 아이콘, 떠 있는 글래스 칩.**
  이 다섯 개가 "AI가 만든 것 같다"의 정체였다(2026-08-20 개편, DECISIONS.md 10번).
  둥근 것은 앱 화면(폰 목업 · 데모 stage)뿐이다. 지면은 각진다 — 이 대비가 앱을 돋보이게 한다
- 테마 2종: 다크(기본 #08090B) / 라이트(웜페이퍼 #F5F2EC, 흰색 아님).
  html[data-theme] 속성과 CSS 변수로 전환. 최초 접속은 반드시 다크
- 색: 지면은 무채색, 강조는 `--mark`(#FF5A2C) 하나. 브랜드 블루·그린은 **앱 UI 안에서만**
  쓴다(링, 안전 구간, 통증 표시). 지면에 블루를 뿌리면 다시 SaaS 랜딩이 된다
- 타이포: 극단적 대비. `.display`는 clamp(38px,6.6vw,94px), 라벨·캡션은 10~11px에 자간 .15~.2em.
  중간 크기로 뭉치면 밋밋해진다
- 사진: `img/`의 스틸만 쓴다. 히어로 영상에서 뽑았고 워터마크를 지웠다(DECISIONS.md 10번).
  외부 인물 사진은 초상권 때문에 금지
- 앱 화면은 `img/app/`. **데모 프로필로 찍은 것만 쓴다** — 실제 아이 이름·학교가 담긴 화면은
  절대 올리지 않는다. `42_my_2`·`54_light_my`는 개발자 모드 줄이 있어 공개 금지(DECISIONS.md 15번)
- 상단 히어로: 영상 2편(0.8배)에 11종목 장면, 카피는 타이머가 아니라 영상 재생 시각을 따라간다.
  종목별 컬러 무드, 이탤릭 초대형 카피, 배경 대형 영문 종목명, 스피드라인, 필름 그레인,
  하단 스토리형 진행 세그먼트 11개(장면 수만큼 JS가 생성). 장면표는 DECISIONS.md 1번
- 섹션은 01~06 번호를 달고, 02(만든 이유) · 06(정보 보호) · 마무리는 풀블리드 도판으로 연다
- **02 만든 이유의 반성문은 실제 창업자의 경험이다.** 아이의 피로 골절과 그날 한 말, 잔소리와
  다툼 — 지어낸 이야기가 아니다. 마케팅 문구로 덧칠하거나 일반적인 브랜드 스토리로 바꾸지 말 것.
  감정을 설명하는 형용사를 더하면 오히려 약해진다. 장면과 사실만 남긴다
- 인터랙티브 데모 4종(루틴 체크리스트/ACWR 슬라이더+몸 그림/실적표 생성/학부모 연동)은
  실제로 조작 가능해야 함
- 가격 표시 절대 금지 — 출시 기념 무료 메시지("무료로 시작하기")만 사용

## 검증

- 수정할 때마다 Playwright(chromium)로 렌더링해 콘솔 에러 0 확인, 스크린샷으로 확인
- 모바일(560px)·태블릿(960px) 반응형 확인
